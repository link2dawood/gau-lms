"""Small helpers shared across modules.

Anything here must be genuinely general: no knowledge of a particular model,
claim or endpoint. Module-specific logic belongs in that module's package
(DECISIONS.md D-024).
"""
